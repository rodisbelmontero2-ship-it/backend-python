# backend-python

PostgreSQL CRUD backend in Python.

## Requisitos
- Python 3.11+
- PostgreSQL 16
- Virtual environment

## Instalación
```bash
python -m venv venv
source venv/Scripts/activate    # Windows (Git Bash)
pip install -r requirements.txt